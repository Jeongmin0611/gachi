<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bitcamp.gachi.classPage.ClassPageDaoImp">
	<select id="classPageAllRecord" resultType="com.bitcamp.gachi.admin.AllVO">
		select category, class_name, real_price, nickname, class_img1 from class join member on class.userid=member.userid
		<choose>
			<when test="category==null"></when>
			<when test="category!=null">
				where category='${param1}'
			</when>
		</choose>
	</select>
</mapper>
